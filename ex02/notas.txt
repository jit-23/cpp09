passar a std::string para nbr

passar para o vt principal que vai ser o OG para cada acao.

aplicar de forma recursiva a separacao das parcelas (Bx, Ax);

dps de entrar em todos os niveis de recursao, aplicar a separacao do OG para os containers 
    main (que inicialmente tera o B1, A1, e os restantes Ax)
    pend (que inicialmente tera os Bx que nao foram incluidos no main), (caso existam parcelas incompletas, estas serao ignoradas e adicionadas ao main no final da insercao do pend em main)

apos separar o main e o pend, aplicaremos Jacobstal nbrs, (0,1,1,3,5,11,21,43 etc) para a insercao do pend para main
    
    Jn = x; J0 = 0; J1 = 1; J2 = 1; J3 = 3; J4 = 5; J5 = 11; J6 = 21; J7 = 43; etc ...

    comecamos sempre por Jn, n = 3 -> J3 = 3, pk? pois iremos usar o numero que o Jn representa para associar ao Bx que existe no pend
    se comecamos por J3 = 3, iremos fazer insercao do B3 que existe no pend, e por logica procuramos o A3 como sabemos que A3 > B3, comparamos entre B1, A1, B2, A2, <- e saltamos as parcelas afrente,
    -> a continuacao com o que estava a falar, se A3 > B3, so precisamos comparar as parcelas anteriores ate encontrar um maior

    como o Jn = 3, tentamos inserir todos os bx menores ou igual a b3, se ficarmos sem Bx para inserir, aumentamos o numero de Jacobstal
    mas SOMENTE se o numero jn+1 for igual ou inferior ao maior numero de Bx no pend, caso tenhamos jn(n = 5) , j4 = 11, e so existem 6 b, nao usaremos os jacobstal para inserir, 
    neste caso especifico iremos inserir as parcelas Bx restantes de forma inversa no main 
    ex:
        pend : b5,b6
        main :      (.........)

        |
        V
        pend : 
        main :      (.........) b6, b5

        aplicar esta logica ate o fim da recursao.